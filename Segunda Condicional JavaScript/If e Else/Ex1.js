let numero = 16; // Você pode mudar o número para testar

if (numero % 4 === 0) {
  console.log("O número " + numero + " é múltiplo de 4.");
} else {
  console.log("O número " + numero + " não é múltiplo de 4.");
}